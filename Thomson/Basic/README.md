# `Thomson/Basic` — the problem

**Reference.** J. J. Thomson (1904); the compactness argument for the existence of a minimiser is
standard.  Blueprint (`docs/blueprint.md`) §1–2.

| file | content |
|---|---|
| `Energy.lean` | `energy`, `Admissible`, `thomsonInf`, invariance under `O(3)`, existence of a minimiser (`thomsonInf_attained`) |
| `ForceBalance.lean` | the first-order condition at a minimiser (`force_balance`), `sum_local_energy`, the covering-radius bound |

Namespace `Thomson`.
