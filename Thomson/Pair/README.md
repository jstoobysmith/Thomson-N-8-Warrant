# `Thomson/Pair` — the pair inequality

**Reference.** `pairP pivots s ≥ 0` on `[0.9619, 2]` from the double zeros at the four chords
(the certificate's tightness) and a one-variable sweep with Taylor lower bounds on a grid;
blueprint §12.5, Task 4.  Certificates GENERATED by `scripts/threepoint/pair_gen.py`;
`decide +kernel` only.

`Coeff.lean` (the polynomial in `w = 1 − s²/2`), `Taylor.lean`, `Tables.lean`, `Sweep*.lean`,
`Main.lean` (`pairP_nonneg_of`).  The strict version used for uniqueness is in
`Thomson/Uniqueness/PairStrict*.lean`.
