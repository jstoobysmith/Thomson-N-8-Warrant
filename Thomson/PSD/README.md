# `Thomson/PSD` — positive semidefiniteness of the blocks

**Reference.** Rational `LDLᵀ` factorisation of `Hp pivotsNum k − λ_k·1` (`λ_k ≥ 5.9·10⁻⁴`) with a
`14·pivotEps` perturbation; blueprint §12.5, Task 2.  All six checks are `decide +kernel`.

`Block0–5.lean` (GENERATED, `scripts/threepoint/psd_gen.py`), `Dom.lean`, `Check.lean`,
`Main.lean` (`certH_posSemidef`).
